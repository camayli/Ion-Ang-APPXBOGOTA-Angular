import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'actividad5-softwarexbogota',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
